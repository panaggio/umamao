-link = url_for(:host => AppConfig.domain, :controller => "users", :action => "show", :id => @user.to_param)
= t('notifier.global.hi', :user => @followed.name)
%br
%br

= t('.message1', :user => @user.name, :app => AppConfig.application_name)
= t('.message2', :user_page => link_to(t('notifier.global.user_page'), link))


%p= simple_format(t('notifier.global.signature'))
