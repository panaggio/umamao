
-if @user
  = t('.message1', :user =>h(@user.name), :email => h(@user.email))
-else
  = t('.message2', :ip => @ip)

= t('.message3', :email => h(@user.email))

= t('.message4', :subject => h(@subject))
==
= @content

