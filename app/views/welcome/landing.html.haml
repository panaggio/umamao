- content_for :header do
  = form_tag new_user_session_path, :id => 'login_form' do
    #email_label.label
      = label_tag 'user[email]', t('users.form.email')
    #password_label.label
      = label_tag 'user[password]', t('users.form.password')

    #email_field
      = email_field_tag 'user[email]', nil, :tabindex => 1
    #password_field
      = password_field_tag 'user[password]', nil, :tabindex => 2
    #submit_button
      = submit_tag t('layouts.application.log_in'), :tabindex => 4

    #remember_me
      = label_tag 'user[remember_me]', t("sessions.new.remember_me")
      = check_box_tag 'user[remember_me]', '1', false, :tabindex => 3

- content_for :js do
  = render :partial => "shared/layout/javascript/landing"

#content
  %h3 Qualquer pergunta, não qualquer resposta.

  %p

    O Umamão é uma base de conhecimento em língua portuguesa
    expandida e melhorada constantemente por acadêmicos e
    profissionais. Temos orgulho de produzir conteúdo de
    qualidade.

  %p
    %strong

      Se você é curioso e gosta de compartilhar conhecimento, o
      Umamão é para você.

#signup
  %h3  #{ t('layouts.application.signup') }

  = form_for @affiliation do |f|
    #landing_signup
      .field
        .label{:style => "position: relative; top: 12px"}
          = f.label :email, "#{t('affiliations.form.email')}:"
          = image_tag('icon_question.gif', :id => 'email-help')
        .input
          = f.email_field :email, :value => "", :tabindex => 5
        #help-box
          %p

            Email acadêmico é o seu email de vínculo com a
            universidade, como seunome@suauniversidade.br.  Se você
            não possui um, deixe o seu email pessoal que entraremos em
            contato.

      .submit
        = image_tag("ajax-loader.gif", :id => "ajax-loader", :class => "hidden")
        = f.submit t('layouts.application.signup_button'), :tabindex => 6
