#topic-suggestions.block.suggestions
  - if !@suggested_topics.blank?
    %h3.title
      = t("followable.suggestions.topics")
      %ul.topic-list
        = render :partial => "topic_list", :locals => {:topics => @suggested_topics, :one_column => true}
